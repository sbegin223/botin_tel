<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Botin Tel" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <header class="bg-indigo-600 text-white shadow">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between h-16 items-center">
          <div class="flex space-x-4">
            <%= link_to t("header.users"), users_path, class: "px-3 py-2 rounded-md text-sm font-medium hover:bg-indigo-700" %>
            <%= link_to t("header.phones"), phones_path, class: "px-3 py-2 rounded-md text-sm font-medium hover:bg-indigo-700" %>
            <%= link_to t("header.employees"), employees_path, class: "px-3 py-2 rounded-md text-sm font-medium hover:bg-indigo-700" %>
            <%= link_to t("header.departments"), departments_path, class: "px-3 py-2 rounded-md text-sm font-medium hover:bg-indigo-700" %>
            <% if current_user&.has_role?("admin") %>
              <%= link_to t("header.roles"), roles_path, class: "px-3 py-2 rounded-md text-sm font-medium hover:bg-indigo-700" %>
            <% end %>
          </div>
    
          <div class="flex items-center space-x-4">
            <span class="text-sm font-medium"><%= t("header.brand") %></span>
    
            <% if logged_in? %>
              <span class="text-sm font-medium"><%= current_user.name %></span>
              <%= button_to t("header.logout"), logout_path, method: :delete,
                  class: "px-3 py-2 rounded-md text-sm font-medium text-white bg-red-600 hover:bg-red-700" %>
            <% else %>
              <%= link_to t("header.login"), login_path,
                  class: "px-3 py-2 rounded-md text-sm font-medium text-white bg-green-600 hover:bg-green-700" %>
            <% end %>
          </div>
    
          <div class="flex items-center space-x-2">
            <% LANGUAGES.each do |name, code| %>
              <%= link_to name, url_for(locale: code),
                  class: "px-3 py-2 rounded-md text-sm font-medium hover:bg-indigo-700" %>
            <% end %>
          </div>
        </div>
      </div>
    </header>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <main class="container mx-auto mt-28 px-5 flex">
      <%= yield %>
    </main>
  </body>
</html>
